export EDITOR="vim"

# ------------------------------------------------------------
# => Alias
# -----------------------------------------------------------
# python
alias python='python3'

# ls
alias l='ls -CF'
alias la='ls -A'
alias ll='ls -AlF'

# rm
alias rm='rm -i'

# docker
alias d="docker"
alias dc="docker-compose"

alias vi="vim"

alias grep='grep --color=auto'
